# Shared VHS settings — not recorded directly. Copy these Set lines into each
# tape, or use `Source _common.tape` if your VHS version supports it.
Set Shell bash
Set FontSize 14
Set Width 1280
Set Height 820
Set Padding 24
Set Theme "Catppuccin Mocha"
Set TypingSpeed 60ms
Set PlaybackSpeed 1.0
