{
  "name": "@glassbox/ui",
  "version": "0.0.0",
  "private": true,
  "description": "The local web dashboard (Vite + React + TS). Reads the local index via the CLI server; nothing leaves the machine. Self-hosted fonts (no CDN).",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "vite build",
    "dev": "vite"
  },
  "dependencies": {
    "@fontsource/plus-jakarta-sans": "^5.2.8",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.21"
  }
}
